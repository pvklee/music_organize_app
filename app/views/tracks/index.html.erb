<h1>Tracks</h1>

<% @tracks.each do |track| %>
<ul>
    <li><%= track.title %></li>
    <li><a href="<%= track_path(track)%>">View track</a></li>
</ul>
<% end %>

<a href="<%= new_track_path %>">Add a new track!</a>